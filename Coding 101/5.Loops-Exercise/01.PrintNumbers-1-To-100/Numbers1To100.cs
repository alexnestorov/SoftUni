using System;

class Program
{
    static void Main()
    {
        int lastNumber = 100;

        for (int sequence = 1; sequence <= lastNumber; sequence++)
        {
            Console.WriteLine(sequence);
        }
    }
}
